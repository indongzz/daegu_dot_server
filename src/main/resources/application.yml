# Connect to MySQL
server:
  address: localhost
  port: 8080

# MySQL setting
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/daegudot?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: ohg!0612
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true # Make DDL and apply to DB
    show-sql: true
